# termi

Multiplayer AI coding terminal — install:

```bash
curl -fsSL https://termi-server.shanunapal.workers.dev/install | sh
```

Then open a new terminal and run `termi` in a project folder, or `termi join <id>` to join a friend's session.

This repo hosts release binaries only.
